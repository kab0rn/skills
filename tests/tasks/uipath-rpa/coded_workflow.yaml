task_id: skill-rpa-coded-workflow
description: >
  Skill-guided evaluation: agent uses the uipath-rpa skill to build a coded
  automation project end-to-end — a Main.cs workflow, a [TestCase] that
  exercises it with Given-When-Then + testing.Verify, and shared Before/After
  hooks via the partial-class CodedWorkflow pattern. Validates the full coded
  surface: project.json wiring, the testing service, the hooks contract, and
  `uip rpa get-errors` as the compile gate.
tags: [uipath-rpa, smoke, coded]

# `uip rpa get-errors` pays a 30–60s Helm cold-start on first call plus
# additional time for package restore. Generating three .cs files and
# validating lands around 10–20 min wall time; these budgets have room.
task_timeout: 1500
agent:
  type: claude-code
  max_turns: 40
  turn_timeout: 900

sandbox:
  driver: tempdir
  python: {}

# Per-task reviewer prompt that contextualizes the CI auth caveat.
# Without this, the reviewer falls back to the experiment default and
# legitimately dings the agent for skipping auth-gated commands —
# those commands genuinely can't run in this sandbox.
llm_reviewer:
  enabled: true
  model: claude-sonnet-4-6
  temperature: 0.0
  max_tokens: 600
  prompt: |
    Evaluate whether the agent built a correct coded UiPath automation
    project for this CI task.

    CI environment caveat: this sandbox has NO UiPath sign-in. Any
    `uip rpa` subcommand that requires Helm/Studio auth (create-project,
    get-errors) will exit non-zero with "HelmFeatureNotAvailableException".
    The task explicitly tells the agent to hand-author project.json and
    treat get-errors exit 1 as expected — so do NOT penalize the agent
    for either. The compile gate is `uip rpa build` (auth-free).

    Focus scoring on:
    - Are the four required files present with the right shape?
      (project.json with CSharp + fileInfoCollection pointing at
      TestLoginSuccess, Main.cs with CodedWorkflow + Execute + Log,
      TestLoginSuccess.cs with [TestCase] + testing.Verify* assertions
      — NOT xUnit/NUnit Assert.*, CodedWorkflowHooks.cs with
      `partial class CodedWorkflow` + IBeforeAfterRun + Before/After)
    - Did `uip rpa build` exit 0? (the one mandatory CI compile gate)
    - Does report.json accurately reflect what the agent did?

    Reply terse, developer-style. Score 1.0 if correct; lower only for
    real issues (wrong files, wrong patterns, build failure). Ignore
    the auth-gated command exit codes.

initial_prompt: |
  Build a coded UiPath automation project called "TestAutomation" that
  exercises three parts of the coded surface in one go: a main workflow,
  a test case that calls it, and shared Before/After hooks that run
  around every test.

  Deliverables, all under ./TestAutomation:
  1. project.json with CSharp expression language and the right entry
     points / fileInfoCollection so the test case is discoverable.
  2. Main.cs — a CodedWorkflow with an Execute method that logs
     "Hello from TestAutomation" using the Log helper.
  3. TestLoginSuccess.cs — a [TestCase] CodedWorkflow using the
     Given-When-Then (Arrange-Act-Assert) structure, invoking the main
     workflow and asserting on its result. Use the UiPath testing
     service methods EXCLUSIVELY for assertions — `testing.VerifyExpression`,
     `testing.VerifyAreEqual`, `testing.VerifyContains`, etc. The skill
     teaches this as the canonical assertion API. Do NOT use xUnit /
     NUnit / MSTest `Assert.*` APIs — those won't integrate with the
     UiPath test runner, and the skill explicitly avoids them.
  4. CodedWorkflowHooks.cs — shared Before/After hooks that apply to
     every test case in the project. Use the partial-class CodedWorkflow
     pattern with IBeforeAfterRun (NOT a base class).

  CI environment note — IMPORTANT: this sandbox has NO UiPath sign-in
  configured. Any `uip rpa` subcommand that needs Studio/Helm auth will
  return "HelmFeatureNotAvailableException (sign-in required)" and exit
  non-zero. This affects `uip rpa create-project`, `uip rpa get-errors`,
  `uip rpa get-default-activity-xaml`, and related Helm-backed tools.
  Do NOT retry those commands when they hit the auth error. Do NOT run
  `uip login` (no creds to sign in with). Do NOT pass `--use-studio`.
  Instead:
    * Hand-author the project scaffolding directly:
        - `TestAutomation/project.json` — follow the skill's project.json
          reference / template. Must set `"expressionLanguage": "CSharp"`
          and declare `TestLoginSuccess.cs` in `fileInfoCollection` with
          `"testCaseType"`.
        - `TestAutomation/project.uiproj` — required by `uip rpa build`
          (without it build fails with "No project.uiproj or
          webAppManifest.json"). Minimal shape:
            {
              "Name": "TestAutomation",
              "ProjectType": "Tests",
              "Description": "Smoke test project",
              "MainFile": "Main.cs"
            }
    * Treat `uip rpa get-errors`' exit 1 as expected and move on — its
      exit code is NOT the CI compile gate.
    * Every `.cs` file MUST declare `namespace TestAutomation;` at the
      top so the synthesized `<ClassName>+<ClassName>Activity` wrappers
      resolve correctly. Missing or mismatched namespaces produce
      CS0234 ("type or namespace does not exist") at build time.

  Validation — run these two commands in this order:

  1. `uip rpa build "$PWD/TestAutomation" --output json`
     (the PROJECT_DIR is positional, NOT `--project-dir`; use the
     absolute path — relative paths like `./TestAutomation` hit a
     path-resolution bug that duplicates the leaf directory.)
     This is the AUTHORITATIVE compile gate in CI. It does NOT need
     Studio/Helm auth and must exit 0 before you declare the task done.
     Fix every compile error it surfaces and rerun until it's clean.
     This is mandatory — not optional.

  2. `uip rpa get-errors --project-dir ./TestAutomation --output json`
     The skill-canonical validation step — call it once for skill
     fidelity, record the exit code, then move on. Its failure due to
     auth is expected and does not block completion. Do NOT try to
     work around the auth error (no `uip login`, no `--use-studio`).

  Save a summary to report.json at the sandbox root:
    {
      "project_name": "TestAutomation",
      "expression_language": "CSharp",
      "files_created": ["<paths of the .cs files you created>"],
      "test_case_class": "<class name of the test case>",
      "hooks_pattern": "<'partial_class' or 'base_class'>",
      "hooks_class_declaration": "<the class declaration line from the hooks file>",
      "assertions_used": ["<list of testing.Verify* methods used>"],
      "commands_used": ["<list of uip commands you ran>"],
      "get_errors_exit_code": <exit code from the final uip rpa get-errors call>,
      "build_exit_code": <exit code from the final uip rpa build call — must be 0>
    }

  Important:
  - The `uip` CLI and the `rpa` tool are already available in the environment.
  - Use `--output json` on any uip commands you run.
  - Do NOT pass `--use-studio` (Studio Desktop is not running interactively).

success_criteria:
  - type: file_exists
    description: "project.json was created"
    path: "TestAutomation/project.json"
    weight: 1.0
    pass_threshold: 1.0

  - type: file_contains
    description: "project.json targets CSharp and references the test case"
    path: "TestAutomation/project.json"
    includes:
      - "CSharp"
      - "TestLoginSuccess"
      - "testCaseType"
    weight: 1.5
    pass_threshold: 1.0

  - type: file_exists
    description: "Main.cs coded workflow file was created"
    path: "TestAutomation/Main.cs"
    weight: 1.0
    pass_threshold: 1.0

  - type: file_contains
    description: "Main.cs is a CodedWorkflow with Execute() that logs the expected message"
    path: "TestAutomation/Main.cs"
    includes:
      - "CodedWorkflow"
      - "Execute"
      - "Hello from TestAutomation"
    weight: 2.0
    pass_threshold: 1.0

  - type: file_exists
    description: "TestLoginSuccess coded test case was created"
    path: "TestAutomation/TestLoginSuccess.cs"
    weight: 1.0
    pass_threshold: 1.0

  - type: file_contains
    description: "Test case uses [TestCase], CodedWorkflow, and testing.Verify* assertions"
    path: "TestAutomation/TestLoginSuccess.cs"
    includes:
      - "[TestCase]"
      - "CodedWorkflow"
      - "testing.Verify"
    weight: 2.0
    pass_threshold: 1.0

  - type: file_exists
    description: "CodedWorkflowHooks.cs was created"
    path: "TestAutomation/CodedWorkflowHooks.cs"
    weight: 1.0
    pass_threshold: 1.0

  - type: file_contains
    description: "Hooks file uses the partial class CodedWorkflow + IBeforeAfterRun pattern"
    path: "TestAutomation/CodedWorkflowHooks.cs"
    includes:
      - "partial class CodedWorkflow"
      - "IBeforeAfterRun"
      - "Before("
      - "After("
    weight: 2.0
    pass_threshold: 1.0

  # Skill-fidelity signal: the skill teaches `uip rpa get-errors` as
  # the canonical validation step, so require the agent to call it.
  # We do NOT gate on its exit code — Helm/Studio auth isn't wired up
  # in CI, so get-errors legitimately exits non-zero even when the code
  # compiles fine.
  - type: command_executed
    description: "Agent ran uip rpa get-errors (skill-canonical validation step)"
    tool_name: "Bash"
    command_pattern: 'uip\s+rpa\s+get-errors'
    min_count: 1
    weight: 1.0
    pass_threshold: 1.0

  # Hard compile gate: `uip rpa build` doesn't need Helm/Studio auth,
  # so in CI it's the authoritative "does this project compile" check.
  # Require it to have returned success.
  - type: command_executed
    description: "Agent ran uip rpa build successfully (compile gate)"
    tool_name: "Bash"
    command_pattern: 'uip\s+rpa\s+build'
    min_count: 1
    require_success: true
    weight: 2.0
    pass_threshold: 1.0

  - type: file_exists
    description: "report.json was created"
    path: "report.json"
    weight: 0.5
    pass_threshold: 1.0

  - type: json_check
    description: "report.json confirms CSharp, partial-class hooks, and a clean compile"
    path: "report.json"
    assertions:
      - expression: "project_name"
        operator: equals
        expected: "TestAutomation"
      - expression: "expression_language"
        operator: equals
        expected: "CSharp"
      - expression: "hooks_pattern"
        operator: equals
        expected: "partial_class"
      - expression: "hooks_class_declaration"
        operator: contains
        expected: "partial class CodedWorkflow"
      # build is the hard compile signal; get-errors is best-effort in
      # CI (Helm auth may be absent) so we don't assert on its exit code.
      - expression: "build_exit_code"
        operator: equals
        expected: 0
      - expression: "length(files_created)"
        operator: gte
        expected: 3
    weight: 1.5
    pass_threshold: 0.8
